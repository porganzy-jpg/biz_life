# 초안 생성만 쓸 거면 아무것도 설치할 필요 없습니다 (표준 라이브러리만 사용).
# 아래는 실제 업로드를 켤 때만 필요합니다.

python-dotenv>=1.0

# 유튜브
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# 인스타그램
requests>=2.31

# 쇼츠 렌더 (10-shorts-factory 와 동일)
edge-tts>=6.1
Pillow>=10.0
imageio-ffmpeg>=0.4      # 시스템에 ffmpeg 이 없을 때 내장 바이너리로 폴백
