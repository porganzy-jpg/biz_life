# PM 검수 — 스프린트 2 (2026-09-20)

## S2-F 사운드 — 판정: **통과**
보고서: `docs/reports/sound_S2.md`, 큐시트 `docs/AUDIO_CUES.md`.

| 체크 | 결과 |
|---|---|
| 산출물 존재 | 앰비언트 3 + SFX 7 + 큐 1 = 11 OGG, 총 1,172KB(≤3MB). ffprobe로 vorbis·길이 확인(앰비언트 50초, 큐 30.8초, SFX ≤0.75초) |
| PM 재현 | 스펙트럼 중심 주파수 직접 계산: 안 1,834Hz < 밖 밤 4,161Hz < 밖 낮 4,618Hz → **눈 감고도 안/밖이 갈린다**(A1). 첫 구현에서 밤이 안보다 어둡게 나온 것을 스스로 잡아 재작업한 과정이 보고서에 있음 |
| 교본 준수 | A1(안/밖 대비)·A2(음악은 드물게: 큐만 music 버스)·A3(물에서 시작, 도리안)·A5(개가 먼저: 0.3~0.5초 선행 요청)·A8(용량·형식)·A9(전부 로컬 합성, 웹 생성 0)·A10(0.8초 무음) 인용·이행. 05 §5 자가 검수 5문항 답변 있음 |
| 소유 영역 | 준수. TASKS 요청함 한 줄만 |
| AI 공개표 | 웹 생성 사용 0 → 공개 대상 없음(07 §6). 좋은 선택 |
| 보여줄 것(W6) | 오디오라 스크린샷 대신 스펙트럼 수치표. 데브로그용 "안/밖 소리 비교" 클립 재료로 충분 |

지적(경미): 사람 귀 청취 검증은 미실시(에이전트 한계) → 사용자가 static/audio를 직접 들어보고 판단. 리더/정원사 시그니처·부족 모티프는 스프린트 3(A4·A7) 후보로 이미 등록.
개발 이관: 볼륨 버스 3개(amb/sfx/music), 트리거 7개(`world:lantern_on` 신규 포함), 문턱 1.5초·밤 3.0초 크로스페이드, 개 경고 선행. → S2-A 월드 에이전트에 전달.

## S2-A 개발(월드) — 판정: **통과**
보고서: `docs/reports/dev_world_S2.md`, 스크린샷 4장 + PM 재현 `pm_world_check.png`.

| 체크 | 결과 |
|---|---|
| PM 재현(브라우저) | 서버 재기동 후 `127.0.0.1:8002/static/world.html` 직접 접속 — **콘솔 에러 0·경고 0**, 우상단 60fps. 화면 왼쪽 절반이 어두운 몰 내부(불 켜진 방 6칸), 오른쪽이 초록 바깥, 그 너머가 종이 안개. **문턱이 화면 한가운데 수직선으로 선다** — 스프린트 목표 "문턱 컷"이 한 장에 들어옴 |
| 첫 말 | 하단에 리더 대사 "불을 켜 두어라. 어둠 속에서도 세는 사람만이 아침에 웃는다." → `/api/ark`의 `voice`가 월드 화면까지 연결됨(첫 30분 대본 1비트 충족) |
| 받은 요청 | S2-D 결함 4건(파사드 방위 +π/2, 4채널 스플랫, 물 z-파이팅, 스팟 y), S2-E 필수 1건(imp_* 40노드 숨김), S2-F 오디오 9이벤트·버스 3개, S2-B `voice` 연결 — **전부 반영**. 파사드 −90°가 실제로 틀렸음을 확인하고 고친 것이 특히 좋다 |
| 소유 영역 | 준수. 신규 2파일만, `server.py`·`app.js`·`data/*` 무수정 |

PM 판단:
1. **좌표계 확정**: 원점 = 몰 문턱 중앙, X∈[−200,200]·Z∈[−125,125], 바깥 = (+X,+Z). `/api/rumors`의 `SPOT_POS` 6개 중 3개가 지형 밖 → 개발(시스템)이 보고서 §3-3 제안표로 교체(스프린트 3).
2. **막힘 1건**: `data/spots.json`을 브라우저가 못 읽어 발견 텍스트가 JS 상수로 하드코딩(D7 위반). `/api/spots` 신설로 해소 — 스프린트 3 필수.
3. 미완(패럴랙스·각인 데이터 연결·저장·A*)은 사유와 함께 §9에 정리됨. 프로토타입 범위로 인정.

## 스프린트 2 회고 3줄
- **잘된 것**: 에이전트끼리 대화하지 않고도 서로의 산출물을 읽고 결함을 잡았다(배경→월드 4건, 시나리오→개발 API 대조). 소유권 분리가 병렬을 만들고, PM 중계가 품질을 만들었다.
- **막힌 것**: 파일 소유권 때문에 데이터가 화면에 닿지 못하는 구간이 생긴다(`spots.json`). 데이터는 전부 API를 통해서만 나가야 한다는 규칙이 필요.
- **다음**: 첫 12분 통합 플레이, `/api/spots`, 좌표 정본화.
## S2-B 개발(시스템) — 판정: **통과**
보고서: `docs/reports/dev_sys_S2.md`, 스크린샷 5장(`docs/reports/sys_S2_*.png`).

| 체크 | 결과 |
|---|---|
| PM 재현(서버 8002) | `load_events()` 38장, 공룡 4장 포함 / 랩터 사건 3회 강제 → 나선 사람 매번 **1명**(정찰병 도담), 「발자국」 각인 1회만(같은 종류 위기는 중복 없음) — S1 결함(전원 진화) 해소 확인 / 스캔 `8801043015097` → `family_name` "붉은 실 가문", 응답 전문에 실제 상표 문자열 0 / `/api/ark` 야간 정원사 대사, 스캔 시 리더 `scan_food` 대사 / `/api/rumors` 6스팟·게이트 확장(food·medical·stationery) 반영·좌표 포함 / `/api/stats` 테스트 uid 15개 제외 |
| 스크린샷 | `sys_S2_morning.png` DAY 2 아침 금색 각인 토스트 + 리더 대사, `sys_S2_event_voice.png` 쪽지 결과에 "나선 사람: 온새(정찰병)" + 각인 + 신뢰 -5 + 리더 대사 — 규칙이 화면에 보인다 |
| 교본 | D4 회귀 7항목 로그, D6 결정성(시드), 참여자 규칙 ④ "카드는 물건이지만 쓴 것은 사람"은 근거 있는 자체 결정(M3) → DECISIONS 등재 |
| 소유 영역 | 준수. `data/imprints.json`을 고치지 않고 런타임 우선 병합으로 시나리오 파일을 이기게 한 선택이 좋다(소유 충돌 0, 시나리오 재수정 자동 반영) |
| 재미 | 사건 결과에 "누가 나섰나"가 생기면서 각인이 사람의 이야기가 됨. 통과 |

PM 결정 2건(DECISIONS 등재):
1. **소문 해금 정본 = `server.py` RUMOR_RULES**. 밸런스는 개발 소유·한 곳 관리. `data/rumors.json`의 `unlock`은 참고 필드로 두고 S3에서 서버 표와 동기화(시나리오가 수치를 바꾸고 싶으면 요청함).
2. 각인 참여자 선별 5순위 규칙 채택(hero → 배치 → 역할 태그 → 카드 태그 역할 → 시드 1명, 상한 2, 기존 부상자·사건 후 합류자 제외).
남은 것: `SPOT_POS` 임시 좌표는 S2-A 지형과 합의(S2-A에 전달), `?debug_force_event=` 배포 전 제거(TASKS 유지).
## S2-C 시나리오 — 판정: **통과**
보고서: `docs/reports/scenario_S2.md`. PM이 JSON 4개를 직접 열어 확인.

| 체크 | 결과 |
|---|---|
| 산출물 | roles_evolved(8, 부족 신화 근거 병기), events_outside(7: 공룡 4·원정/스팟 3), rumors(20, 스팟 6곳·출처 5종·카테고리 8종 전부), imprint_lines(8, 감각만·수치 0), 두 AI 첫 대면 대본, 첫 30분 43비트 표 |
| 품질 | 진화 이름이 성경의 신화에서 직접 나옴(정찰병=노래잡이 ← 걷는 노래, 의무병=숨의 손 ← 진흙과 숨의 실제 자리). 랩터 카드 "불을 켤 것인가, 숨을 죽일 것인가"가 S3(사건은 질문) 정확히 이행. 각인 문장은 `{name}의 …` 형태로 조사 버그 회피(세심) |
| 검증 | JSON 유효·스키마 통과·id 38개 중복 0·금지어 112개 스캔 0. **실서버에서 /api/rumors 동작까지 대조**(음료 3회 → 물에 잠긴 전철 해금) — 시나리오가 개발 결과를 검증한 좋은 협업 |
| 교본 | P1~P5·S1~S10·W1/W6/W7 인용, 01 §5 자가 검수 답, 이탈 1건 사유(스팟 카드 positive 미표기 — 각인 경로 유지 목적) 명시 |
| 소유 영역 | 준수 |

PM 판단 3건:
1. **공룡 카드 미병합(막힘)** → S2-B 개발에 즉시 전달: `load_events()` 파일 목록화(`events.json`, `events_tribes.json`, `events_outside.json`), 진화 이름·각인 문장 교체, RUMOR_RULES 확장, when 태그 2개 승인, 테스트 uid 제외.
2. **실제 상표가 가문 이름으로 노출**(`known_families.json`의 "농심 가문" 등) → 규칙 위반 확정(00_COMMON §5). 결정: 화면에 뜨는 가문 이름은 **우리가 지은 이름**으로, 실제 제조사는 숨김 필드(`real`)로만 유지. 시나리오에 가문 이름 18개 제작 요청, 개발이 적용.
3. `cold_snap`→「지킨 자」 오매칭은 각인 표 9종 확장이 필요 → 스프린트 3 후보로 등록, DECISIONS 거쳐 성경 개정.

추가 납품 검수(가문 이름): `data/family_names.json` 18개 — PM 재현: 키 집합이 `known_families.json` 접두어 18개와 완전 일치, 실제 상표 18종 문자열 검색 0건, 이름이 `relic_templates.json` 유물 어휘와 맞물림(붉은 실 가문 ↔ 말린 실). **통과**. 개발(S2-B)에 적용 요청 전달됨. "없는 접두어 = 이름 없는 가문이 정상"이라는 시나리오의 제안은 MYTH_ORIGINS §8과 결이 맞아 채택.
## S2-D 배경 — 판정: **통과**
보고서: `docs/reports/bg_S2.md`, 렌더 `art_raw/world/mall_facade_front.png`·`spot_flooded_train.png`.

| 체크 | 결과 |
|---|---|
| PM 재현(파일) | 텍스처 4장 512² — 가장자리 픽셀 차이(L/R·T/B)가 내부 인접 차이보다 작음(grass 4.1/2.1 vs 11.4, water 0.1 vs 1.1) → 이음새 없음 수치로 확인. GLB 1,240KB·1,301KB(≤1.5MB). 스팟 재질 26종에 fabric/pillow/tarp/fire/bedroll 0. 노드 `marker_threshold`·`marker_light_shaft`·`koi_1~5`·`keep_water_surface` 존재 |
| PM 재현(눈) | 파사드 입면: 깨진 멀리언 격자·기울어진 간판·말린 셔터·덩굴이 설명 없이 읽힘(W6 스크린샷 합격). 스팟: 청록 수면 위 멈춘 객차, 손잡이 꽃, 빛기둥 구멍 2개, 왼쪽 아래 **문간이 유일한 마른 자리**로 보임 — 시나리오 요청 3건 반영 확인. 스카이라인 6랜드마크(굴뚝·슬래브·시계탑·온실 볼트·탑·터널 아치) 실루엣만으로 구분 |
| 아트 원칙 | B2 팔레트(아스팔트 R=G=B 완전 무채, 물 청록=정원사) 준수. 이탈 2건(B1 등불 파사드 미적용, B4 야영 흔적 0)은 "바깥엔 사람 불이 없다"·B10 거주 금지 근거 → 인정(M3) |
| 소유 영역 | 준수. `blender_iso.py` import 재사용으로 중복 0, pantry 회귀 diff None. 신규 `tools/blender_scenes.py` |
| 협업 | 병렬로 나온 `world3d.js`를 읽고 통합 결함 4건(파사드 −90° 방향, 스플랫 1장만 사용, 물 2장 겹침, 스팟 y 오프셋)을 미리 잡아냄 — 이것이 이번 스프린트 최고의 검수 기여 |

지적(경미, S3): 파사드 입면이 회색 비중이 높아 초록이 점처럼만 보임 — 엔진 안에서 지형 초록·담쟁이 밀도로 보완되는지 통합 때 확인. 스카이라인은 가로 반복 불가(넓은 맵에선 양끝 페이드 필요). 유리 알파가 Three에서 불투명하면 `transparent=true` 강제.
개발 이관: §7-7 4건 → S2-A에 전달.
## S2-E 캐릭터 — 판정: **통과**
보고서: `docs/reports/char_S2.md`, 스크린샷 `char_S2_show.png`·`char_S2_rowJ.png`·`char_S2_beasts.png`.

| 체크 | 결과 |
|---|---|
| PM 재현(GLB JSON 직접 파싱) | 짐승 3종 175~192KB, 공룡 3종 178~216KB, 전부 Idle/Walk 2클립·스킨 1. 공룡 정점 y 범위 0.01~5.06(트렉스)·0.00~1.80(랩터)·0.01~3.20(트리케) → 발 원점·실측 키 일치. 8역할 GLB에 `imp_*` 노드 40개씩, 클립 7종 유지, 용량 674~861KB |
| PM 재현(눈) | J행 각인 9컷: 포자 반점·뺨 흉터·베일·젖은 머리·이빨 목걸이·검은 팔띠·허리 유골·놋쇠 팔찌가 각각 다른 부위에 있고 3개 겹침 컷에서 충돌 없음. 각인이 "몸에 남은 이야기"로 읽힘(GROWTH_AND_MYTH 계단식 성장의 시각화) |
| 발견의 질 | glTF에 가시성 필드가 없다는 사실을 빈 큐브 실험으로 확인하고 개발 필수 요청으로 올림 / NLA 트랙이 기본 포즈를 망가뜨리는 문제·Quaternius 액션의 오브젝트 scale 키 문제를 스스로 잡음 — 재현 가능한 기술 지식이 보고서에 남았다(W1) |
| 소유 영역·공개표 | 준수. 생성 AI 0 → 공개표 갱신 불필요 |

PM 정정 1건: 역할 이름 불일치(수집가·경비·기록자·상인)는 **PM의 중계 메시지 오타**였다. 데이터는 일치한다 — `roles.json`과 `roles_evolved.json` 모두 scout/cook/medic/engineer/farmer/scholar/trader/kid(정찰병·요리사·의무병·기술자·농부·학자·교섭가·아이). `evo_<role>`의 `<role>`은 이 id로 확정. 요청함 해당 줄 해소.
개발 이관(필수): 역할 GLB 로드 직후 `imp_*` 전부 숨김 → S2-A에 전달(world.html이 역할 GLB를 쓰는 유일한 화면).
지적(경미, S3): 이빨 목걸이가 흰 조리복 위에서 대비 낮음, 등 뒤 파츠는 정면 컷에서 작음 → 각인 파츠 색을 역할 의상 팔레트의 보색 쪽으로.
